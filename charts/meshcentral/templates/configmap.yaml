apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-default
data:
  OPENFRAME_MODE: "true"
  OPENFRAME_GATEWAY_URL: "localhost"

  MESH_PROTOCOL: "ws"
  MESH_DEVICE_GROUP: "OpenFrame"
  MESH_EXTERNAL_PORT: {{ .Values.config.settings.port | default 4430 | quote }}
 
